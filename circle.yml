dependencies:
  pre:
    - wget https://storage.googleapis.com/golang/go1.5.linux-amd64.tar.gz; sudo tar -C /usr/local -xzf go1.5.linux-amd64.tar.gz; export GOROOT=/usr/local/go; export PATH=$GOROOT/bin:$PATH